[package]
name = "prokadiff-evidence"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "RA / MC / JC consensus evidence (clean-room vs breseq methods)"
readme.workspace = true

[dependencies]
prokadiff-gd = { workspace = true }
thiserror = { workspace = true }
# BAM/SAM I/O only (AGENTS Rule 9). FASTA uses a tiny local parser so we do not
# pull noodles-fasta into the pileup path.
noodles = { workspace = true }
rayon = { workspace = true }
